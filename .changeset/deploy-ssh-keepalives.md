---
"@razroo/ray": patch
---

Repo-only: add OpenSSH keepalives to VPS deploy SSH and rsync sessions, and guard workflow SSH timeout settings so connected deploy sessions cannot hang indefinitely after a network stall.
