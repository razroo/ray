---
"@razroo/ray-core": minor
"@razroo/ray-sdk": minor
---

Add async durable inference jobs with `POST /v1/jobs`, deterministic `seed` support, `stop` and `responseFormat` request controls, OpenAI-compatible `warmupRequests`, and token-aware scheduler budgets.
