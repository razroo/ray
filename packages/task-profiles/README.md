# @razroo/ray-task-profiles

Task execution profiles for lean single-model Ray deployments.

```ts
import { resolveTaskProfileRequest } from "@razroo/ray-task-profiles";
```

Use this package to apply classify, summarize, draft, revise, and JSON extraction profiles to normal Ray inference requests.
