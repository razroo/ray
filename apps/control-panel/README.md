# Control Panel

This stays intentionally empty in the first cut.

Ray should earn a UI after the single-node runtime, configuration model, and deployment workflow are stable. Until then, the gateway, docs, and example configs are the product surface.

